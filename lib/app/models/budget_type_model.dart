class BudgetType {
  BudgetType({
    this.title,
    this.subtitle,
    this.isSelected,
    this.minPrice,
    this.maxPrice,
  });
  String title;
  String subtitle;
  bool isSelected;
  double minPrice;
  double maxPrice;
}
